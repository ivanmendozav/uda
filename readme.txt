Para instalar extensión "matplotlib" desde línea de comandos (Cmd):
ivanm>py -m pip install matplotlib

Para instalar extensión de github "utm" desde línea de comandos (Cmd):
py -m pip install git+https://github.com/Turbo87/utm.git